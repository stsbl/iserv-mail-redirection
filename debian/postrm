#!/bin/bash
set -e
shopt -s nullglob

if [ "$1" = "remove" ] || [ "$1" = "purge" ]
then
  rm -rf /var/cache/iserv/stsbl-iserv-mail-redirection
  rm -f /var/lib/iserv/stsbl-iserv-mail-redirection/app/cachedir
fi

#DEBHELPER#
